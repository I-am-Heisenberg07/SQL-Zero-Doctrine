{
  "name": "sql-formatter",
  "displayName": "SQL Formatter - Zero Doctrine",
  "description": "Formats SQL stored procedures and queries using the Zero Doctrine ruleset",
  "version": "1.0.0",
  "publisher": "Harsh",
  "author": {
    "name": "Harsh"
  },
  "icon": "icon.png",
  "license": "MIT",
  "engines": {
    "vscode": "^1.110.0"
  },
  "categories": ["Formatters"],
  "keywords": ["sql", "formatter", "zero doctrine", "stored procedure"],
  "activationEvents": [
    "onLanguage:sql",
    "onLanguage:tsql",
    "onLanguage:mssql",
    "onLanguage:sql-ms"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sqlZeroDoctrine.formatFile",
        "title": "Format Document (Zero Doctrine)",
        "category": "SQL Zero Doctrine",
        "icon": "$(database)"
      },
      {
        "command": "sqlZeroDoctrine.formatSelection",
        "title": "Format Selection (Zero Doctrine)",
        "category": "SQL Zero Doctrine"
      },
      {
        "command": "sqlZeroDoctrine.reportFeedback",
        "title": "Report Bug / Request Feature",
        "category": "SQL Zero Doctrine",
        "icon": "$(feedback)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "sqlZeroDoctrine.formatFile",
          "when": "resourceExtension == sql || resourceExtension == SQL || editorLangId == sql || editorLangId == tsql || editorLangId == mssql || editorLangId == sql-ms",
          "group": "1_modification@1"
        },
        {
          "command": "sqlZeroDoctrine.formatSelection",
          "when": "(resourceExtension == sql || resourceExtension == SQL || editorLangId == sql || editorLangId == tsql || editorLangId == mssql || editorLangId == sql-ms) && editorHasSelection",
          "group": "1_modification@2"
        },
        {
          "command": "sqlZeroDoctrine.reportFeedback",
          "when": "resourceExtension == sql || resourceExtension == SQL || editorLangId == sql || editorLangId == tsql || editorLangId == mssql || editorLangId == sql-ms",
          "group": "1_modification@3"
        }
      ]
    },
    "keybindings": [
      {
        "command": "sqlZeroDoctrine.formatFile",
        "key": "ctrl+shift+alt+f",
        "when": "editorTextFocus && (resourceExtension == sql || editorLangId == sql || editorLangId == tsql || editorLangId == mssql || editorLangId == sql-ms)"
      },
      {
        "command": "sqlZeroDoctrine.formatSelection",
        "key": "ctrl+shift+alt+g",
        "when": "editorTextFocus && editorHasSelection && (resourceExtension == sql || editorLangId == sql || editorLangId == tsql || editorLangId == mssql || editorLangId == sql-ms)"
      },
      {
        "command": "sqlZeroDoctrine.reportFeedback",
        "key": "ctrl+shift+alt+r",
        "when": "editorTextFocus && (resourceExtension == sql || editorLangId == sql || editorLangId == tsql || editorLangId == mssql || editorLangId == sql-ms)"
      }
    ],
    "configuration": {
      "title": "SQL Zero Doctrine",
      "properties": {
        "sqlZeroDoctrine.inListBreakAt": {
          "type": "number",
          "default": 4,
          "description": "Number of items in an IN(...) list before it breaks to multiple lines. Default: 4"
        },
        "sqlZeroDoctrine.maxInlineColLen": {
          "type": "number",
          "default": 80,
          "description": "Column character length threshold before switching to long-column treatment. Default: 80"
        },
        "sqlZeroDoctrine.reorderJoinOn": {
          "type": "boolean",
          "default": false,
          "description": "When true, rewrites ON conditions so the joined table appears on the left side. Default: false"
        },
        "sqlZeroDoctrine.userName": {
          "type": "string",
          "default": "",
          "description": "Your name — added to the modification header when formatting. Leave blank to skip the header."
        },
        "sqlZeroDoctrine.stripCommentedCode": {
          "type": "boolean",
          "default": false,
          "description": "When true, removes lines that are clearly commented-out SQL code (SELECT, FROM, WHERE, JOIN, etc.). Descriptive comments, change notes and section labels are always preserved."
        }
      }
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/I-Am-Walter-White/SQL-Zero-Doctrine.git"
  },
  "scripts": {
    "test": "node formatter.test.js",
    "package": "npx @vscode/vsce package"
  }
}